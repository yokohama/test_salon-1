<%= link_to "新規作成", new_staffs_category_path %>
<br>
<br>
<% @categories.each do |category| %>
  <%= link_to "#{category.name}", staffs_category_path(category.id)%>/<%= link_to "削除", staffs_category_path(category.id), method: :delete, data: { confirm: '削除しますか？' } %>
<br>
<% end %>
