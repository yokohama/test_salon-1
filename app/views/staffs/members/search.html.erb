<%= paginate @results %>
<table border="1">
    <tr>
      <th>id</th>
      <th>nickname</th>
      <th>email</th>
    </tr>

    <% @results.each do |result| %>
      <tr>
        <td><%= result.id %></td>
        <td><%= result.nickname %></td>
        <td><%= result.email %></td>
      </tr>
    <% end %>
</table>
<br>
<%= link_to 'member一覧', staffs_members_path %>
<%= paginate @results %>
